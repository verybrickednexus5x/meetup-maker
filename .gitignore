# Env files — never commit real credentials, use .env.example as the template
.env
.env.*
!.env.example

# Auto-generated by @tanstack/router-plugin on every dev/build run
src/routeTree.gen.ts

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# This project uses bun (see bunfig.toml + bun.lock) — don't commit npm's lockfile
package-lock.json

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
